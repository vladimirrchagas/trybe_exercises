const upperCase = string => string.toUpperCase();

const firstChar = string => string.charAt(0);

const concatTwoStrings = (string1, string2) => string1 + string2;

module.exports = { upperCase, firstChar, concatTwoStrings };